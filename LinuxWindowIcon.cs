namespace Glyphore;

internal static class LinuxWindowIcon
{
    public static void Initialize()
    {
        // Majorsilence.Forms/Avalonia owns the native window. The icon is already
        // assigned through the cross-platform Form.Icon property by MainForm.
    }
}
